name: flutter_form_validation
description: A new Flutter project.
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  equatable: ^2.0.3
  flutter:
    sdk: flutter
  formz: ^0.6.0
  get: ^4.6.1

flutter:
  uses-material-design: true
